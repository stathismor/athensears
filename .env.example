# Strapi CMS Secrets (generate new ones for production!)
APP_KEYS=key1,key2,key3,key4
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Gig Crawler API Key
# Brave AI Grounding API: https://api.search.brave.com/app/dashboard
# Free tier: 2,000-5,000 queries/month (search + AI extraction combined)
# Requires credit card for verification (no charge on free tier)
BRAVE_API_KEY=your_brave_api_key_here

# Strapi API Token for Gig Crawler
# Create this in Strapi admin panel (Settings > API Tokens)
# Needs permissions: gig.create, gig.find, venue.create, venue.find
STRAPI_API_TOKEN=your_strapi_api_token_here
