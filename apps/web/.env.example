# Strapi CMS URL used by the server-rendered web app.
#
# The page fetches gigs from Strapi server-side on each request, so a single URL
# covers every environment - no public/internal split. Point it at the internal/
# private CMS URL in production. Defaults to http://localhost:1337 for local dev.
STRAPI_API_URL=http://localhost:1337
